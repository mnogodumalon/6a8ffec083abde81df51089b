Legend: `!` after a field name = required (from the app's base view) —
a step form that writes such a field must not submit it empty.
`^` after a field name = shown in the app's list view: the fields users
recognize a record by — prefer them for select-card titles, stats and recall texts.
A field's human label follows in quotes where it adds information (`kosten[number] "Kosten (EUR)"`) —
use the label in every user-facing text, never the identifier.
A lookup field lists its keys after the type (`[lookup/select: a|b|c]`);
these are the ONLY valid write values — quote them into the brief verbatim;
`key=Label` shows the human label where it differs (write the key, show the label).
`·` lines carry a field's description from the app model — real process
knowledge: use it for step texts and "why we ask this" hints.
`≡` = how many records the app held when this was built — CONTEXT only
(spot empty entities, size the flow). It never chooses the data path: every
pick-a-record step goes through useRecordSearch, which counts at RUNTIME and
switches to server-side search when the entity has grown since the build
(check-intents 3k rejects an EntitySelectStep fed from a useDashboardData array).
`◈` names the input FORM each field shape wants — range → calendar
(AvailabilityRangePicker via form.range), choice → pills (ChoiceGroup), record →
select step (EntitySelectStep), stock → show the quantity, warn on overshoot.
Pick the form that fits the data; a plain field only when nothing better exists.
`⚙` lists the service methods for that entity, spelled exactly.
`✚` lists the entity's Enriched* fields and its enrich() signature,
spelled exactly — quote both verbatim; an invented enriched field or
a missing map is a guaranteed type error.

Modules — copy these paths verbatim into a subagent brief:
  @/types/app                            entity types, APP_IDS, LOOKUP_OPTIONS
  @/types/enriched                       EnrichedMitgliederKursanmeldung, EnrichedKursanmeldungFuerInteressenten — the COMPLETE list, no other Enriched* type exists
  @/lib/enrich                           the enrich functions — exact fields and signatures on the `✚` line of each entity below; every listed map is REQUIRED, and no other enriched field exists
  @/services/livingAppsService           LivingAppsService, createRecordUrl, extractRecordId
  @/hooks/useDashboardData               useDashboardData — the RAW arrays (one per entity, camelCase), their setters, loading, error, fetchAll + kursverwaltungMap, mitgliederverwaltungMap; it carries NO enriched arrays — build those with the `✚` call below
  @/lib/formatters                       lookupKey, formatDate
  @/lib/journey                          useStepForm, useJourneySubmit, makeReference, SHAPES, type JourneyPort, useRecordSearch (searchFields, toItem, filter, where), fieldText, fieldLookup, fieldNumber, fieldDate, fieldRef, resolveSelectMode — validation, plan runner, reference; door-agnostic
  @/services/journeyPort                 servicePort — the INTERNAL door of the journey port (list · create · ref); hand it to useJourneySubmit and to blocks, never call LivingAppsService for a create the plan can do
  @/lib/polish                           undoToast — success toast with an undo action (every delete in a flow)
  @/components/blocks/IntentWizardShell  IntentWizardShell, useWizard — forms=, draftKey=, intro=, steps[i].enabledIf
  @/components/blocks/StepNav            StepNav — Back / Continue with the next step's name; onNext returns form.validate(...)
  @/components/blocks/SummaryStep        SummaryStep — check-answers step before EVERY write (forms=, submit=)
  @/components/blocks/SuccessStep        SuccessStep — final step from submit.result: reference, facts, next actions
  @/components/blocks/ChoiceGroup        ChoiceGroup — pill radiogroup for a lookup with few options ({...form.choice(key)})
  @/components/blocks/EntitySelectStep   EntitySelectStep — onSelect, selectedId; adapts cards/search-first to the count; ALWAYS fed via {...x.select} from useRecordSearch
  @/components/blocks/StatusBadge        StatusBadge, getStatusColor
  @/components/blocks/BudgetTracker      BudgetTracker
  @/components/blocks/AvailabilityRangePicker  AvailabilityRangePicker, rangeIsFree, isNightBlocked ({...form.range(from, to, { blocked })})
There is NO `@/config/appIds`, no `@/config/lookupOptions` and no
`@/lib/urlUtils` — APP_IDS and LOOKUP_OPTIONS come from `@/types/app`,
createRecordUrl from the service. Note the lowercase `l` in the service path.

- Kursverwaltung (kursverwaltung): kursname!^[string/text], beschreibung^[string/textarea], kursdatum!^[date/datetimeminute] "Kursdatum und Uhrzeit", ort^[string/text] "Veranstaltungsort", max_teilnehmer^[number] "Maximale Teilnehmerzahl"
  ≡ 3 records at build time
  · kursdatum: A specific moment in time consisting of year, month, day, hour and minute (or `null`/`None`) The field value will be a string in the format `<YYYY>-<MM>-<DD>…
  ⚙ getKursverwaltung() · getKursverwaltungEntry(id) · createKursverwaltungEntry(fields) · updateKursverwaltungEntry(id, fields) · deleteKursverwaltungEntry(id) · queryKursverwaltung({ filter, orderby, limit, offset, fields }) · countKursverwaltung(filter?)
- Mitgliederverwaltung (mitgliederverwaltung): vorname!^[string/text], nachname!^[string/text], geburtsdatum^[date/date], email^[string/email] "E-Mail-Adresse", telefon^[string/tel] "Telefonnummer", strasse[string/text], hausnummer[string/text], postleitzahl[string/text], ort[string/text], beitragsart![lookup/select: foerdermitglied|jugendmitglied|ehrenmitglied|familienmitglied|vollmitglied], mitgliedschaft_von![date/date] "Mitgliedschaft gültig von", mitgliedschaft_bis[date/date] "Mitgliedschaft gültig bis", bemerkungen[string/textarea]
  ≡ 3 records at build time
  ◈ mitgliedschaft_von+mitgliedschaft_bis → range candidate — YOU decide in .intents-staging/occupancy.json whether it is a stay, which applookup is the booked resource, which status keys do not occupy (AvailabilityRangePicker, form.range; blocked = occupancyFor()) · beitragsart → choice (5 options, ChoiceGroup)
  ⚙ getMitgliederverwaltung() · getMitgliederverwaltungEntry(id) · createMitgliederverwaltungEntry(fields) · updateMitgliederverwaltungEntry(id, fields) · deleteMitgliederverwaltungEntry(id) · queryMitgliederverwaltung({ filter, orderby, limit, offset, fields }) · countMitgliederverwaltung(filter?)
- Mitglieder-Kursanmeldung (mitglieder_kursanmeldung): mitglied!^[applookup/select], kurs!^[applookup/select], anmerkungen^[string/textarea]
  ≡ 7 records at build time
  ◈ mitglied → record (mitgliederverwaltung, EntitySelectStep) · kurs → record (kursverwaltung, EntitySelectStep)
  ⚙ getMitgliederKursanmeldung() · getMitgliederKursanmeldungEntry(id) · createMitgliederKursanmeldungEntry(fields) · updateMitgliederKursanmeldungEntry(id, fields) · deleteMitgliederKursanmeldungEntry(id) · queryMitgliederKursanmeldung({ filter, orderby, limit, offset, fields }) · countMitgliederKursanmeldung(filter?)
  ✚ EnrichedMitgliederKursanmeldung adds ONLY mitgliedName, kursName — via enrichMitgliederKursanmeldung(mitgliederKursanmeldung, { mitgliederverwaltungMap, kursverwaltungMap }), all maps required
  → mitglied links to app_id 6a8ffd927dd69a8d9165d82d
  → kurs links to app_id 6a8ffd8f3c521d7f844024a3
- Kursanmeldung für Interessenten (kursanmeldung_fuer_interessenten): kurs!^[applookup/select], vorname!^[string/text], nachname!^[string/text], email!^[string/email] "E-Mail-Adresse", telefon^[string/tel] "Telefonnummer"
  ≡ 4 records at build time
  ◈ kurs → record (kursverwaltung, EntitySelectStep)
  ⚙ getKursanmeldungFuerInteressenten() · getKursanmeldungFuerInteressentenEntry(id) · createKursanmeldungFuerInteressentenEntry(fields) · updateKursanmeldungFuerInteressentenEntry(id, fields) · deleteKursanmeldungFuerInteressentenEntry(id) · queryKursanmeldungFuerInteressenten({ filter, orderby, limit, offset, fields }) · countKursanmeldungFuerInteressenten(filter?)
  ✚ EnrichedKursanmeldungFuerInteressenten adds ONLY kursName — via enrichKursanmeldungFuerInteressenten(kursanmeldungFuerInteressenten, { kursverwaltungMap }), all maps required
  → kurs links to app_id 6a8ffd8f3c521d7f844024a3